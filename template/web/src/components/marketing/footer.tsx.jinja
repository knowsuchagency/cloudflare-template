export function Footer() {
  return (
    <footer className="border-t border-[var(--ink)] py-6">
      {/* ASCII separator row — apex chrome */}
      <div
        aria-hidden="true"
        className="mb-3 overflow-hidden font-mono text-[11px] tracking-[0.04em] text-[var(--ink-ghost)] whitespace-nowrap"
      >
        {"*".repeat(220)}
      </div>

      {/* Single mono footer row, uppercase, ghosted */}
      <div className="flex flex-wrap items-center justify-between gap-x-4 gap-y-2 font-mono text-[10px] tracking-[0.08em] uppercase text-[var(--ink-ghost)]">
        <span>
          (c) {new Date().getFullYear()} · {{ app_title }} · FUTURE OPERATIONS DEPARTMENT
        </span>
        <span className="flex items-center gap-3">
          <a href="#" className="hover:text-[var(--ink)]">
            PRIVACY
          </a>
          <span aria-hidden>·</span>
          <a href="#" className="hover:text-[var(--ink)]">
            SECURITY
          </a>
          <span aria-hidden>·</span>
          <a href="#" className="hover:text-[var(--ink)]">
            CHANGELOG
          </a>
          <span aria-hidden>·</span>
          <a href="#" className="hover:text-[var(--ink)]">
            STATUS
          </a>
          <span aria-hidden>·</span>
          <a href="#" className="hover:text-[var(--ink)]">
            GITHUB
          </a>
        </span>
        <span>V.01</span>
      </div>
    </footer>
  )
}
