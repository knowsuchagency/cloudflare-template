// biome-ignore-all lint/a11y/useValidAnchor: DOCS is an intentional placeholder link in the template; wire up a real href per project

import { Link } from "react-router"

import { Button } from "@/components/ui/button"
import { Wordmark } from "@/components/marketing/wordmark"
import { ThemeToggle } from "@/components/theme-toggle"

export function Nav() {
  return (
    <nav className="flex items-center justify-between border-b border-[var(--border-1)] py-5">
      <Link to="/" className="no-underline">
        <Wordmark text="{{ app_title }}" size="md" />
      </Link>

      <div className="flex items-center gap-5 font-mono text-[11px] tracking-[0.1em] uppercase">
        <a
          href="#features"
          className="hidden text-[var(--fg-2)] no-underline transition-colors duration-150 hover:text-[var(--fg-0)] sm:inline"
        >
          FEATURES
        </a>
        <span aria-hidden className="hidden text-[var(--fg-3)] sm:inline">·</span>
        <a
          href="#"
          className="hidden text-[var(--fg-2)] no-underline transition-colors duration-150 hover:text-[var(--fg-0)] sm:inline"
        >
          DOCS
        </a>
        <span aria-hidden className="hidden text-[var(--fg-3)] sm:inline">·</span>
        <a
          href="https://github.com"
          target="_blank"
          rel="noreferrer"
          className="hidden text-[var(--fg-2)] no-underline transition-colors duration-150 hover:text-[var(--fg-0)] sm:inline"
        >
          GITHUB
        </a>
        <ThemeToggle />
        <Button variant="outline" size="sm" asChild>
          <Link to="/login">SIGN IN</Link>
        </Button>
        {/* Monochrome here so the hero's GET STARTED owns the page's one red. */}
        <Button variant="default" size="sm" asChild>
          <Link to="/signup">
            SIGN UP <span aria-hidden>→</span>
          </Link>
        </Button>
      </div>
    </nav>
  )
}
