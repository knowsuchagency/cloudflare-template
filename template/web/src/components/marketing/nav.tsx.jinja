import { Link } from "react-router"

import { Button } from "@/components/ui/button"
import { Wordmark } from "@/components/marketing/wordmark"

export function Nav() {
  return (
    <nav className="flex items-center justify-between border-b border-[var(--ink)] py-5">
      <Link to="/" className="no-underline">
        <Wordmark text="{{ app_title }}" size="md" />
      </Link>

      <div className="flex items-center gap-5 font-mono text-[11px] tracking-[0.08em] uppercase">
        <a
          href="#features"
          className="text-[var(--ink)] no-underline transition-colors duration-[120ms] hover:text-[var(--orange)]"
        >
          FEATURES
        </a>
        <span aria-hidden className="text-[var(--ink-ghost)]">·</span>
        <a
          href="#"
          className="text-[var(--ink)] no-underline transition-colors duration-[120ms] hover:text-[var(--orange)]"
        >
          DOCS
        </a>
        <span aria-hidden className="text-[var(--ink-ghost)]">·</span>
        <a
          href="https://github.com"
          target="_blank"
          rel="noreferrer"
          className="text-[var(--ink)] no-underline transition-colors duration-[120ms] hover:text-[var(--orange)]"
        >
          GITHUB
        </a>
        <Button variant="outline" size="sm" asChild>
          <Link to="/login">SIGN IN</Link>
        </Button>
        <Button variant="accent" size="sm" asChild>
          <Link to="/signup">
            SIGN UP <span aria-hidden>→</span>
          </Link>
        </Button>
      </div>
    </nav>
  )
}
